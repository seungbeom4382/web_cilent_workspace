tailwind.config = {
    theme: {
      extend: {
        colors: {
          clifford: '#da373d',
        },
        fontFamily : {
          'sans' : ['Dongle', 'sans-serif'] // 기본 설정
        }
      }
    }
  }